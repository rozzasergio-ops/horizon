{
  "name": "horizon",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@horizon/web",
    "build": "npm run build --workspace=@horizon/web",
    "start": "npm run start --workspace=@horizon/web",
    "lint": "npm run lint --workspace=@horizon/web"
  },
  "engines": {
    "node": ">=20"
  }
}
